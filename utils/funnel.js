/**
 * utils function to build the Funnel json object from databases rows.
 * @rows list of rows (resultset)
 */
const buildFunnel = () => {
  const funnelJSON = null;
  return funnelJSON;
};

const funnelUtils = {
  buildFunnel,
};

module.exports = funnelUtils;
